You are a senior frontend engineer and UI/UX designer. Improve the **Knowledge Notes editor screen** shown in the attached reference screenshot.

### IMPORTANT — DO NOT CHANGE EXISTING FUNCTIONALITY

This is an existing working application.

**Do NOT replace, redesign, remove, or modify the existing editor implementation or any integrated editor tools.**

The existing editor toolbar must remain exactly as it is functionally and visually. This includes all existing:

* Bold
* Italic
* Strikethrough
* Headings
* Bullet list
* Numbered list
* Checklist
* Quote
* Code
* Links
* Images
* Tables
* Undo/Redo
* Preview
* Markdown/editor behavior
* Python execution
* Save functionality
* Existing editor library/components

Do not rebuild the editor.

The goal is to improve the **UI surrounding the editor** so the screen feels cleaner, less crowded, more professional, and easier to use.

---

## Current problems

The current screen has too many competing controls around the editor:

* Rename and Delete are always visible
* There are multiple Delete buttons
* Save competes with other actions
* Chapter actions (`+`, edit, delete) are always visible
* The header consumes too much vertical space
* The editor toolbar is surrounded by too much UI
* Preview / Vertical / Horizontal controls feel crowded
* The chapter sidebar has unnecessary visual noise
* The document title and notebook title hierarchy is unclear

---

# Required UI changes

## 1. Keep the existing editor toolbar untouched

This is the highest priority.

Do not modify the toolbar component.

Do not remove buttons.

Do not reorder editor tools.

Do not replace the editor library.

Do not change editor behavior.

Simply position the existing toolbar more cleanly within the page.

---

## 2. Simplify the document header

Change the surrounding header toward this hierarchy:

```text
← Notes / System Design                         ⋯

Test                                      Save
```

The document title `Test` should be the primary visual element.

`System Design` should feel like contextual breadcrumb/navigation information.

Do not add unnecessary large headings.

---

## 3. Move Rename and Delete into an overflow menu

Do not show Delete prominently twice.

Use a single `⋯` menu.

Example:

```text
⋯
──────────────
Rename
Duplicate
Delete
```

Keep all existing functionality.

Delete should remain visually destructive when opened, but should not dominate the normal editor screen.

If Rename currently uses existing functionality, continue using that same implementation.

---

## 4. Keep Save

Do NOT remove the existing Save behavior.

Keep a clear Save button near the document title.

Example:

```text
Test                              [Save] [⋯]
```

If the existing application already has save-state information, display it subtly without removing Save.

Example:

```text
Saved ✓
```

or

```text
Unsaved changes    [Save]
```

Do not implement a new autosave system unless one already exists.

---

## 5. Improve the Python controls

Keep the existing Python functionality.

Do not change the execution implementation.

Present the controls more clearly:

```text
⚡ Python enabled        [Run Python]
```

or, when disabled:

```text
○ Python disabled       [Enable]
```

The existing "Disable run" behavior should remain available.

The goal is only better visual hierarchy.

---

## 6. Keep the chapter sidebar

Do NOT remove or replace the chapter functionality.

Keep:

* Add chapter
* Select chapter
* Rename/edit chapter
* Delete chapter
* Existing chapter data
* Existing chapter behavior

But reduce visual clutter.

Instead of permanently displaying:

```text
First                  + ✎ 🗑
Test                   + ✎ 🗑
Phase 1 — LLM...       + ✎ 🗑
```

show:

```text
First
Test                         ⋯
Phase 1 — LLM Foundations    ⋯
```

The `⋯` actions can appear on hover/focus.

The selected chapter should remain clearly highlighted.

Do not change the existing chapter data model.

---

## 7. Improve chapter sidebar spacing

Keep the sidebar approximately the same functional width, but improve:

* Padding
* Typography
* Row height
* Selected state
* Truncation
* Hover states
* Separation between chapter title and metadata

Avoid excessive borders.

The sidebar should feel like navigation, not another large card.

---

## 8. Improve Preview controls without removing functionality

Keep the existing Preview functionality.

Instead of visually presenting:

```text
Preview    Vertical    Horizontal
```

make the primary mode concept clearer:

```text
Edit    Split    Preview
```

If Vertical/Horizontal are existing split-layout controls, show them only when Split mode is active:

```text
Edit    Split    Preview

         Vertical    Horizontal
```

Do not remove the underlying Vertical/Horizontal functionality.

Do not change the preview renderer.

---

## 9. Reduce unnecessary vertical space

The editor should begin significantly higher on the page.

The target structure should be approximately:

```text
Top application header
        ↓
Breadcrumb / document header
        ↓
Document title + actions
        ↓
Python controls
        ↓
Existing editor toolbar
        ↓
Editor
```

Avoid multiple large empty header rows.

The editor should receive the majority of the viewport height.

---

## 10. Make the layout responsive

Desktop:

```text
┌──────────────┬──────────────────────────────────┐
│ Chapters     │ Document                         │
│              │                                  │
│              │ Existing editor                  │
└──────────────┴──────────────────────────────────┘
```

Allow the chapter sidebar to collapse if the application already supports sidebar state.

When collapsed, the editor should receive the additional width.

Do not break the existing editor dimensions or functionality.

---

# Visual direction

Aim for:

* Clean
* Minimal
* Professional
* Calm
* Productivity-focused
* Knowledge-management feel
* Strong visual hierarchy
* Less visual noise
* More whitespace
* Subtle borders
* Existing orange accent retained
* Destructive red reserved for destructive actions
* Existing design language preserved

Do NOT introduce a completely new design system.

This should feel like an evolution of the existing LifeOS UI, not a different application.

---

# Important interaction principle

Use **progressive disclosure**.

Frequently used actions should remain visible.

Secondary actions should appear on hover/focus or inside `⋯`.

Do not remove functionality just to make the UI look cleaner.

The user should still be able to perform every operation they can perform today.

---

# Do not do these things

DO NOT:

* Replace the editor
* Replace the editor toolbar
* Remove editor toolbar buttons
* Remove Preview
* Remove Python
* Remove Save
* Remove chapters
* Remove chapter actions
* Remove Rename
* Remove Delete
* Remove existing functionality
* Introduce a new editor library
* Rewrite the data model
* Change API behavior
* Change routing
* Change persistence behavior
* Change Markdown behavior
* Change Python execution behavior

Only improve the presentation and interaction hierarchy around the existing functionality.

---

# Target result

The final screen should feel approximately like:

```text
┌───────────────────────────────────────────────────────────────┐
│ Knowledge Notes                              Search  Assistant │
├───────────────────────────────────────────────────────────────┤
│                                                               │
│ ← Notes / System Design                              ⋯        │
│                                                               │
│ ┌───────────────────┐                                         │
│ │ CHAPTERS       +  │   Test                       Save  ⋯   │
│ │                   │                                         │
│ │ First             │   ⚡ Python enabled     Run Python      │
│ │                   │                                         │
│ │ ▌ Test         ⋯  │   Existing editor controls             │
│ │                   │   ────────────────────────────────────  │
│ │ Phase 1...     ⋯  │                                         │
│ │                   │   Existing editor                       │
│ └───────────────────┘                                         │
│                                                               │
│                                                               │
└───────────────────────────────────────────────────────────────┘
```

The exact implementation should follow the existing project's architecture, components, styles, and conventions.

Before modifying code, inspect the existing implementation and identify the components responsible for:

1. Knowledge page header
2. Chapter sidebar
3. Chapter actions
4. Document title/actions
5. Python controls
6. Editor wrapper
7. Preview controls
8. Save/delete/rename actions

Then make the **smallest clean refactor necessary** to achieve the UI improvements above.

Prioritize **UI polish without regression**.