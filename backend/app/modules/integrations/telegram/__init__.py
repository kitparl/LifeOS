"""Telegram interactive UI kernel — presentation only, no business logic.

Submodules are imported directly (e.g. telegram.client, telegram.renderer)
to avoid circular imports when client/config live under this package.
"""
